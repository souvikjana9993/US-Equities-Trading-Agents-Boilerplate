# Copy this file to UI/.env for local runs.
# Keep UI/.env out of git because it contains a real API key.
LITELLM_BASE_URL="https://api.ai-gateway.example.com/v1"
LITELLM_API_KEY="********"
LITELLM_MODEL="gpt-5-nano"
